#include <bits/stdc++.h>
using namespace std;
#define vi vector<int>
#define pii pair<int, int>
#define rep(i, a, b) for (int i = a; i < b; i++)
#define ff first
#define ss second

int main()
{
    // Hashing is used for accessing elements of an array(large size eg. 10^8 elements) using key value pair
    int arr[] = {11, 12, 13, 14, 15};
    // H(x) = [x % 10]

    // hash table will be :
    // [0, 1, 2, 3, 4 ,5]
    // [0, 11,12,13,14,15]
    return 0;
}